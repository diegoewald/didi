import { ShieldCheck } from 'lucide-react';
import { routes } from '../../app/routes';

export function Sidebar({ current, navigate }: { current: string; navigate: (path: string) => void }) {
  return <aside className="fixed left-0 top-0 z-30 hidden h-screen w-72 flex-col border-r border-slate-200 bg-white/85 p-5 backdrop-blur-xl dark:border-slate-800 dark:bg-slate-950/85 lg:flex"><button onClick={() => navigate('/forense')} className="flex items-center gap-3 text-left"><div className="rounded-2xl bg-gradient-to-br from-teal-500 to-indigo-600 p-3 text-white"><ShieldCheck/></div><div><h1 className="text-xl font-black">DIDI Forensics</h1><p className="text-xs font-bold text-teal-600">imagem • ruído • metadados</p></div></button><nav className="mt-8 space-y-1 overflow-auto pb-6">{routes.map(route => <button key={route.path} onClick={() => navigate(route.path)} className={`flex w-full items-center gap-3 rounded-2xl px-4 py-3 text-left font-bold transition ${current === route.path ? 'bg-slate-900 text-white shadow-xl dark:bg-white dark:text-slate-950' : 'text-slate-600 hover:bg-slate-100 dark:text-slate-300 dark:hover:bg-slate-900'}`}><route.icon size={19}/>{route.label}</button>)}</nav><div className="mt-auto rounded-3xl bg-teal-50 p-4 text-sm text-teal-900 dark:bg-teal-950 dark:text-teal-100"><b>Importante:</b> o relatório é uma estimativa probabilística. Nenhum detector deve ser usado como prova única de que uma imagem foi gerada por IA.</div></aside>;
}
