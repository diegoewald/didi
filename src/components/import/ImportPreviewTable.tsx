import type { Transaction } from '../../types';
import { formatCurrency, formatDateBR } from '../../lib/formatters/formatters';
export function ImportPreviewTable({ rows }: { rows: Transaction[] }) { return <div className="overflow-auto rounded-3xl border border-slate-200 dark:border-slate-800"><table className="w-full min-w-[760px] text-sm"><thead className="bg-slate-100 text-left dark:bg-slate-900"><tr><th className="p-3">Data</th><th>Descrição</th><th>Tipo</th><th>Categoria</th><th>Valor</th><th>Status</th></tr></thead><tbody>{rows.slice(0,50).map(r=><tr key={r.id} className="border-t border-slate-100 dark:border-slate-800"><td className="p-3">{formatDateBR(r.date)}</td><td>{r.description}</td><td>{r.type}</td><td>{r.category}</td><td className="font-black">{formatCurrency(r.value)}</td><td>{r.status}</td></tr>)}</tbody></table></div>; }
