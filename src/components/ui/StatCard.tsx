import type { LucideIcon } from 'lucide-react';
import { ArrowDownRight, ArrowUpRight } from 'lucide-react';
interface Props { title: string; value: string; hint?: string; icon: LucideIcon; trend?: number; tone?: 'green'|'red'|'blue'|'amber'|'slate'; }
export function StatCard({ title, value, hint, icon: Icon, trend, tone='blue' }: Props) { const map = { green:'from-emerald-500 to-teal-500', red:'from-rose-500 to-orange-500', blue:'from-blue-500 to-indigo-500', amber:'from-amber-500 to-orange-500', slate:'from-slate-600 to-slate-800' }; return <section className="card animate-rise p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-bold text-slate-500 dark:text-slate-400">{title}</p><h3 className="mt-2 text-2xl font-black tracking-tight">{value}</h3>{hint && <p className="mt-1 text-xs text-slate-500">{hint}</p>}</div><div className={`rounded-2xl bg-gradient-to-br ${map[tone]} p-3 text-white shadow-lg`}><Icon size={22}/></div></div>{trend !== undefined && <div className="mt-4 flex items-center gap-2 text-sm font-bold"><span className={trend>=0?'text-emerald-600':'text-rose-600'}>{trend>=0?<ArrowUpRight size={16}/>:<ArrowDownRight size={16}/>}</span>{Math.abs(trend).toFixed(1)}% vs. mês anterior</div>}</section>; }
