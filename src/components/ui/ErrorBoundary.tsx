import { Component, type ErrorInfo, type ReactNode } from 'react';
export class ErrorBoundary extends Component<{ children: ReactNode }, { error?: string }> { state: { error?: string } = { error: undefined }; static getDerivedStateFromError(error: Error): { error: string } { return { error: error.message }; } componentDidCatch(error: Error, info: ErrorInfo) { console.error('FinançasPro capturou erro:', error, info); } render() { if (this.state.error) return <main className="grid min-h-screen place-items-center p-6"><div className="card max-w-lg p-8 text-center"><h1 className="text-2xl font-black">Algo não saiu como esperado</h1><p className="mt-2 text-slate-500">A aplicação foi protegida contra a falha. Recarregue a página ou restaure um backup.</p><pre className="mt-4 overflow-auto rounded-2xl bg-slate-100 p-3 text-left text-xs dark:bg-slate-900">{this.state.error}</pre></div></main>; return this.props.children; } }
