import { AlertTriangle, BadgeDollarSign, Banknote, PiggyBank, TrendingDown, TrendingUp, Wallet } from 'lucide-react';
import { Cell, Line, LineChart, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis, CartesianGrid } from 'recharts';
import { ChartCard } from '../../components/ui/ChartCard';
import { StatCard } from '../../components/ui/StatCard';
import { useFinanceStore } from '../transactions/store';
import { categoryExpensePercentages, financialSummary, monthlyEvolution, overdueBills, pendingBills } from '../../lib/calculations/finance';
import { formatCurrency, formatDateBR } from '../../lib/formatters/formatters';
export function DashboardPage() { const { transactions, budgets } = useFinanceStore(); const summary = financialSummary(transactions); const evolution = monthlyEvolution(transactions).map(m=>({ ...m, income:Number(m.income), expense:Number(m.expense), net:Number(m.net) })); const categories = categoryExpensePercentages(transactions); const pending = pendingBills(transactions).slice(0,5); const overdue = overdueBills(transactions); const top = transactions.filter(t=>t.type==='Despesa').sort((a,b)=>Number(b.value)-Number(a.value)).slice(0,10); const previous = Number(summary.previousMonthNet); const current = Number(summary.monthlyNet); const trend = previous === 0 ? 0 : ((current - previous) / Math.abs(previous)) * 100; const alerts = [...(overdue.length ? [`${overdue.length} conta(s) atrasada(s) precisam de atenção.`] : []), ...(budgets.length ? [] : ['Defina orçamentos mensais por categoria para receber alertas de 80% e 100%.']), ...(summary.savingsRate < 10 ? ['Taxa de economia abaixo de 10%; revise despesas variáveis.'] : ['Boa taxa de economia neste mês.'])]; return <div className="space-y-6"><div className="flex flex-col justify-between gap-3 md:flex-row md:items-end"><div><h1 className="text-3xl font-black">Dashboard financeiro</h1><p className="text-slate-500">Visão executiva com caixa, competência, saúde financeira e alertas inteligentes.</p></div><div className="rounded-full bg-white px-4 py-2 text-sm font-black shadow dark:bg-slate-900">Saúde: {summary.healthScore}/100</div></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><StatCard title="Saldo atual" value={formatCurrency(summary.balance)} icon={Wallet} tone="blue" hint="Somente lançamentos pagos afetam o caixa real"/><StatCard title="Receitas do mês" value={formatCurrency(summary.monthlyIncome)} icon={TrendingUp} tone="green"/><StatCard title="Despesas do mês" value={formatCurrency(summary.monthlyExpense)} icon={TrendingDown} tone="red"/><StatCard title="Resultado líquido" value={formatCurrency(summary.monthlyNet)} icon={PiggyBank} tone={current>=0?'green':'red'} trend={trend}/></div><div className="grid gap-6 xl:grid-cols-3"><ChartCard title="Evolução mensal" subtitle="Receitas, despesas e resultado líquido"><div className="h-80"><ResponsiveContainer><LineChart data={evolution}><CartesianGrid strokeDasharray="3 3" opacity={0.25}/><XAxis dataKey="month"/><YAxis tickFormatter={(v)=>`R$${Number(v)/1000}k`}/><Tooltip formatter={(v)=>formatCurrency(Number(v))}/><Line type="monotone" dataKey="income" stroke="#10b981" strokeWidth={3} name="Receitas"/><Line type="monotone" dataKey="expense" stroke="#ef4444" strokeWidth={3} name="Despesas"/><Line type="monotone" dataKey="net" stroke="#6366f1" strokeWidth={3} name="Resultado"/></LineChart></ResponsiveContainer></div></ChartCard><ChartCard title="Gastos por categoria" subtitle="Distribuição de despesas"><div className="h-80"><ResponsiveContainer><PieChart><Pie data={categories} dataKey="value" nameKey="category" innerRadius={62} outerRadius={105} paddingAngle={4}>{categories.map((_,i)=><Cell key={i} fill={['#14b8a6','#f97316','#6366f1','#ef4444','#8b5cf6','#22c55e'][i%6]}/>)}</Pie><Tooltip formatter={(v)=>formatCurrency(Number(v))}/></PieChart></ResponsiveContainer></div></ChartCard><section className="card p-5"><h3 className="text-lg font-black">Alertas inteligentes</h3><div className="mt-4 space-y-3">{alerts.map(a=><div key={a} className="flex gap-3 rounded-2xl bg-amber-50 p-3 text-sm font-bold text-amber-900 dark:bg-amber-950 dark:text-amber-100"><AlertTriangle size={18}/>{a}</div>)}</div><div className="mt-5 grid grid-cols-2 gap-3"><div className="rounded-2xl bg-slate-100 p-3 dark:bg-slate-900"><p className="text-xs text-slate-500">Taxa de economia</p><b>{summary.savingsRate.toFixed(1)}%</b></div><div className="rounded-2xl bg-slate-100 p-3 dark:bg-slate-900"><p className="text-xs text-slate-500">Mês atual x anterior</p><b>{trend.toFixed(1)}%</b></div></div></section></div><div className="grid gap-6 xl:grid-cols-2"><section className="card p-5"><h3 className="mb-4 text-lg font-black"><BadgeDollarSign className="mr-2 inline"/>Top 10 maiores despesas</h3><div className="space-y-2">{top.map(t=><div key={t.id} className="flex items-center justify-between rounded-2xl bg-slate-50 p-3 dark:bg-slate-900"><div><b>{t.description}</b><p className="text-xs text-slate-500">{t.category} • {formatDateBR(t.date)}</p></div><b>{formatCurrency(t.value)}</b></div>)}</div></section><section className="card p-5"><h3 className="mb-4 text-lg font-black"><Banknote className="mr-2 inline"/>Próximas contas a vencer</h3><div className="space-y-2">{pending.map(t=><div key={t.id} className="flex items-center justify-between rounded-2xl bg-slate-50 p-3 dark:bg-slate-900"><div><b>{t.description}</b><p className="text-xs text-slate-500">Vence em {formatDateBR(t.dueDate)}</p></div><b>{formatCurrency(t.value)}</b></div>)}</div></section></div></div>; }
